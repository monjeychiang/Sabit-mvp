# 日誌等級（可選：DEBUG、INFO、WARNING、ERROR、CRITICAL）
LOG_LEVEL=INFO

# 主密碼（請於生產環境設為強密碼）
SABIT_MASTER_PASSWORD=your_master_password

# 資料庫連線字串（預設為 SQLite，本地開發可用）
DATABASE_URL=sqlite+aiosqlite:///data/sabit_local.db

# API 伺服器設定
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# CORS 設定（允許的來源，多個來源用逗號分隔）
CORS_ORIGINS=http://localhost:5173

# 交易所連線設定
EXCHANGE_CONNECTION_TTL=3600  # 連線存活時間（秒）

# 時間同步設定
TIME_SYNC_INTERVAL=3600  # 時間同步間隔（秒）
PREFERRED_TIME_SERVICE=google  # 優先使用的時間服務（google 或 binance） 